import { ScrollView, View } from "react-native";
import { GridDemo } from "../examples/GridDemo/GridDemo";

export default function Index() {
  return (
    <ScrollView>
      <GridDemo />
    </ScrollView>
  );
}
