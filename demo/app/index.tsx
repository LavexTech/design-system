import { Text, View, ScrollView } from "react-native";
import { ButtonDemo } from "../examples/ButtonDemo/ButtonDemo";

export default function Index() {
  return (
    <ScrollView>
      <ButtonDemo />
    </ScrollView>
  );
}
